This program is to accept a string as an input from the user and to input a font (among 3 given fonts), and then the String 
will be displayed to the user with the desired font.

print_alpha.c - contains the array which holds all the letters of each font (A-Z in capital letters in three different font sizes)

char_set.c - contains the actual input/output generation process.
